'use client';
import styled from 'styled-components';

export const SBlogCardsContainer = styled.div`
  max-width: 1200px;
  //margin: 0 auto;
  //display: grid;
  //grid-gap: 32px;
  //grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
  //grid-template-columns: repeat(auto-fit, minmax(100%, 1fr));
`;
