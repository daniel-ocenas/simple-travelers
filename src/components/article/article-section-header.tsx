'use client';

import React from 'react';

const ArticleSectionHeader = (props: any) => {
  return <h3 className={'mb-4 text-xl font-bold'}>{props.text}</h3>;
};

export default ArticleSectionHeader;
